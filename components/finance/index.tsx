import { Badge } from "@/components/ui";
export function Amount({ value, currency = "EUR" }: { value?: number | null; currency?: string }) { return <span>{new Intl.NumberFormat("nl-NL", { style: "currency", currency }).format(value ?? 0)}</span>; }
export function CommissionStatusExplain({ status }: { status: "pending" | "available" | "paid" | "reversed" }) { const text = { pending: "Wacht op geverifieerde betaling en levering.", available: "Beschikbaar voor een volgende uitbetaling.", paid: "Uitbetaald door VDB Digital Software.", reversed: "Teruggedraaid vanwege refund of correctie." }[status]; return <div className="flex items-center gap-3"><Badge className={status === "paid" ? "border-emerald-500/40 text-emerald-300" : ""}>{status}</Badge><span className="text-sm text-muted">{text}</span></div>; }
