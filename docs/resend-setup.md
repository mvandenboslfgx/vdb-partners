# Resend setup
Create and verify a sending domain in Resend, store `RESEND_API_KEY` as a server-only secret, and enable `FEATURE_RESEND` only after domain verification. Use named transactional templates, minimal personal data, and monitored retry/failure handling.

Do not send production email from a development environment. Define opt-in/transactional classification and retention with legal review.
