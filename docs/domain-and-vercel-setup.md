# Domain and Vercel setup
Link the repository to the intended Vercel project, configure preview and production environment variables separately, and assign the approved portal domain after DNS verification. Enforce HTTPS and configure the Mollie webhook against the production domain only after validation.

Verify deployment protection, access ownership, secret rotation, logs, backups, and rollback procedure before launch.
