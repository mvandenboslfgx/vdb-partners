"use client";
import { useState } from "react";
import { Logo, PaymentRuleBanner } from "@/components/brand";
import { Button, Card, Input, Label } from "@/components/ui";

const steps = [["Welkom", "Vertel ons hoe wij u kunnen bereiken."], ["Bedrijf", "Vul uw bedrijfsgegevens aan."], ["Verificatie", "Verifieer uw identiteit zodra de provider is geconfigureerd."], ["Overeenkomst", "Lees en accepteer de partnerovereenkomst."]];
export default function OnboardingPage() { const [step, setStep] = useState(0); const [done, setDone] = useState(false); const current = steps[step]; return <main className="mx-auto max-w-2xl px-5 py-10"><Logo /><div className="mt-12 flex gap-2">{steps.map((_, i) => <span key={i} className={`h-1 flex-1 ${i <= step ? "bg-gold" : "bg-white/10"}`} />)}</div><Card className="mt-8 p-7"><p className="text-xs uppercase tracking-[.16em] text-gold">Stap {step + 1} van {steps.length}</p><h1 className="display mt-3 text-4xl">{done ? "Aanmelding ingediend" : current[0]}</h1><p className="mt-2 text-sm text-muted">{done ? "Wij beoordelen uw gegevens. U ontvangt een bericht zodra u kunt starten." : current[1]}</p>{!done && <div className="mt-7 space-y-4">{step === 0 && <><div><Label>Naam</Label><Input placeholder="Uw naam" /></div><div><Label>E-mailadres</Label><Input type="email" placeholder="naam@bedrijf.nl" /></div></>}{step === 1 && <><div><Label>Bedrijfsnaam</Label><Input /></div><div><Label>KvK-nummer</Label><Input /></div></>}{step === 2 && <PaymentRuleBanner />}{step === 3 && <><PaymentRuleBanner /><label className="mt-4 flex gap-3 text-sm text-muted"><input type="checkbox" className="accent-[#c5a572]" /> Ik accepteer de partnerovereenkomst.</label></>}<div className="mt-6 flex justify-between"><Button variant="ghost" onClick={() => setStep(Math.max(0, step - 1))} disabled={step === 0}>Terug</Button><Button onClick={() => step === steps.length - 1 ? setDone(true) : setStep(step + 1)}>{step === steps.length - 1 ? "Indienen" : "Volgende"}</Button></div></div>}</Card></main>; }
