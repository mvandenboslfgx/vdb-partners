# VDB brand assets

Place the approved VDB logo in this directory as `vdb-logo.svg` or `vdb-logo.png`.
Do not replace this instruction with an unapproved recreation, and update the application references only after the asset is supplied.
